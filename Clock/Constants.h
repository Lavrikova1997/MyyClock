#pragma once

const float COEF_HOUR = 30.f;
const float COEF_MINUTE = 6.f;
const float COEF_SECOND = 6.f;
const float CENTER_CLOCK = 150.f;
const int SIZE_CLOCK_WINDOW = int(CENTER_CLOCK * 2);
const float RADIUS_CENTRAL_POINT = 1.f;